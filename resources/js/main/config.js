const APP_URL = 'http://localhost:8000/'

export const base_url = (uri = '') => {
  return APP_URL + uri
}

export const setUrlParams = (url, param, value) => {
  if( url.indexOf('?') == -1 ){
    
  }else{

  }
}